{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/Musqet/ucp-lightning-spec/refs/tags/v2026-04-28/lightning/lnurl-pay.config.json",
  "title": "Lightning LNURL-pay Handler Config",
  "description": "Configuration for the com.musqet.lnurl-pay payment handler. The Business advertises an LNURL-pay endpoint or a Lightning Address; the Platform executes the LUD-06 flow to obtain a BOLT11. The endpoint MUST support LUD-12 comments with commentAllowed large enough to hold the checkout_id.",
  "type": "object",
  "oneOf": [{ "required": ["lightning_address"] }, { "required": ["lnurl"] }],
  "properties": {
    "lightning_address": {
      "type": "string",
      "pattern": "^[^@\\s]+@[^@\\s]+\\.[^@\\s]+$",
      "description": "Lightning Address in the form user@host. Resolves to https://{host}/.well-known/lnurlp/{user}. The resolved endpoint MUST support LUD-12 comments with commentAllowed large enough to hold the checkout_id."
    },
    "lnurl": {
      "type": "string",
      "pattern": "^(lnurl1[a-z0-9]+|https://.+)$",
      "description": "LNURL-pay endpoint — either lowercase bech32-encoded (lnurl1...) or a direct https:// URL. The endpoint MUST support LUD-12 comments with commentAllowed large enough to hold the checkout_id."
    },
    "display_name": {
      "type": "string",
      "description": "Optional human-readable label for payment-method UI."
    }
  }
}
