{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/Musqet/ucp-lightning-spec/refs/tags/v2026-04-23/lightning/instrument.json",
  "title": "Lightning Payment Instrument",
  "description": "Payment instrument shared across the com.musqet.* handler family. Carries a preimage credential proving Lightning Network settlement.",
  "allOf": [
    { "$ref": "https://ucp.dev/2026-04-08/schemas/shopping/types/payment_instrument.json" }
  ],
  "type": "object",
  "required": ["id", "handler_id", "type", "credential"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Platform-assigned instrument identifier."
    },
    "handler_id": {
      "type": "string",
      "description": "Business-assigned handler instance id. MUST match the id of a handler instance under one of the com.musqet.* families in the Business's payment_handlers."
    },
    "type": {
      "const": "com.musqet.preimage"
    },
    "credential": {
      "$ref": "https://raw.githubusercontent.com/Musqet/ucp-lightning-spec/refs/tags/v2026-04-23/lightning/credential.json"
    },
    "selected": {
      "type": "boolean",
      "description": "Set to true when this instrument is the one the Platform is submitting in a checkout completion."
    },
    "billing_address": {
      "$ref": "https://ucp.dev/2026-04-08/schemas/shopping/types/postal_address.json",
      "description": "Not used for payment verification (Lightning has no AVS). MAY be provided when the Business requires a billing or shipping address for order fulfillment."
    },
    "display": {
      "type": "object",
      "description": "Optional display metadata for Platform UIs.",
      "properties": {
        "label": {
          "type": "string",
          "description": "Human-readable label, e.g. 'Lightning Network'."
        },
        "icon_url": {
          "type": "string",
          "format": "uri",
          "description": "URL to an icon representing the Lightning Network payment method."
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
